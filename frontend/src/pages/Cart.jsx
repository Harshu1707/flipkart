import { useEffect } from 'react';
import { Link } from 'react-router-dom';
import { useDispatch, useSelector } from 'react-redux';
import { fetchCart, removeCart, updateCart } from '../features/cartSlice.js';
export default function Cart() { const dispatch = useDispatch(); const items = useSelector((s) => s.cart.items); useEffect(() => { dispatch(fetchCart()); }, [dispatch]); const total = items.reduce((s, i) => s + i.price * i.quantity, 0); return <div className="grid gap-6 md:grid-cols-[1fr_320px]"><section className="card divide-y">{items.map((i) => <div key={i.id} className="flex gap-4 p-4"><img src={i.image} className="h-24 w-24 object-contain" /><div className="flex-1"><b>{i.name}</b><p>₹{Number(i.price).toLocaleString('en-IN')}</p><input type="number" min="1" className="mt-2 w-20 rounded border p-1" value={i.quantity} onChange={(e) => dispatch(updateCart({ id: i.id, quantity: Number(e.target.value) }))} /></div><button onClick={() => dispatch(removeCart(i.id))} className="text-red-600">Remove</button></div>)}</section><aside className="card h-fit p-5"><h2 className="text-xl font-bold">Price details</h2><p className="my-4 flex justify-between"><span>Total</span><b>₹{total.toLocaleString('en-IN')}</b></p><Link to="/checkout" className="btn-primary block text-center">Place order</Link></aside></div>; }
