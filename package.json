{
  "name": "flipkart-inspired-commerce",
  "version": "1.0.0",
  "private": true,
  "description": "Production-ready Flipkart-inspired full-stack ecommerce app",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "npm run start --prefix backend",
    "build": "npm run build --prefix frontend",
    "test": "npm run lint --prefix frontend && npm run lint --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
