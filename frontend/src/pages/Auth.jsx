import { useState } from 'react';
import toast from 'react-hot-toast';
import { useDispatch } from 'react-redux';
import { Link, useNavigate } from 'react-router-dom';
import { login, register } from '../features/authSlice.js';
export default function Auth({ mode }) { const isRegister = mode === 'register'; const [form, setForm] = useState({ name: '', email: '', password: '' }); const dispatch = useDispatch(); const navigate = useNavigate(); async function submit(e) { e.preventDefault(); try { await dispatch((isRegister ? register : login)(form)).unwrap(); toast.success('Welcome'); navigate('/'); } catch (err) { toast.error(err.message || 'Authentication failed'); } } return <form onSubmit={submit} className="card mx-auto max-w-md space-y-4 p-8"><h1 className="text-2xl font-bold">{isRegister ? 'Create account' : 'Login'}</h1>{isRegister && <input className="w-full rounded border p-3" placeholder="Name" onChange={(e) => setForm({ ...form, name: e.target.value })} />}<input className="w-full rounded border p-3" placeholder="Email" onChange={(e) => setForm({ ...form, email: e.target.value })} /><input type="password" className="w-full rounded border p-3" placeholder="Password" onChange={(e) => setForm({ ...form, password: e.target.value })} /><button className="btn-primary w-full">{isRegister ? 'Register' : 'Login'}</button><p>{isRegister ? 'Already registered?' : 'New here?'} <Link className="text-flipkart-blue" to={isRegister ? '/login' : '/register'}>{isRegister ? 'Login' : 'Register'}</Link></p></form>; }
